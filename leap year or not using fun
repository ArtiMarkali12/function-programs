include<stdio.h>
int main()
{
        int year;
        int leap(int year);
        printf("enter year:");
        scanf("%d",&year);
        leap(year);
        }
        int leap(int year)
        {
        if(year%4==0)
        {
        printf("year is leap year");
        }
        else

        printf("Year is not leap year:");
        }

